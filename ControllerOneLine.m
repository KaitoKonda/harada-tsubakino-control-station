function commands = ControllerOneLine(vehicles)
%CONTROLLERONELINE Formation-controller placeholder; currently commands a halt.
% Implement the formation law here without changing main or transport code.
    commands = ControllerStop(vehicles);
end
